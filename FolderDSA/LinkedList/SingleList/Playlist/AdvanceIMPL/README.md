## A working Playlist 
- This is a playlist that works.
- It implements the knowledge of singly Linked list.
- It access the file path and play the music in that folder and loads them into the linked list.
- NB Songs with the extension .wav.
- It has a SongNode with attributes filepath and next (SongNode).
- Uses inner class
- Include the methods for adding songs, displaying Songs, geting the song.
