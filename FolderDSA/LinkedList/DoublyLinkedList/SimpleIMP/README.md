## Simple Doubly linked list 
- This program implement the doubly linked list.
- Each node store data of type int and contains an address of prev and next node, but the tail next address is null.
- It contains methods to either add at the begging of the list or at the end and delete at the begining or end.
