## Browser Navigation
- This is a program that implements the doubly Linked List for navigating to previous and next page in a browser.
- It ask for url from the user, then checks if the URL is valid before browsing to that page.
- If the user types "prev" or "next" in order to navigate to the page that was before and after that current page respectively.
- The url's are stored in a doubly linked list.
